# 🚀 دليل النشر الشامل على Vercel وربط السيرفر السحابي (Vercel Deployment Guide)

هذا الدليل يشرح لك خطوة بخطوة كيفية نشر **OmniOCR Pro** كمنصة ويب احترافية عالمية وسريعة جداً.

---

## 💡 فهم البنية المعمارية للنشر (Architecture Overview)

* **الواجهة الأمامية (Frontend)**: واجهة مستخدم تفاعلية فائقة السرعة (`public/index.html`) يتم رفعها على **Vercel** للاستفادة من شبكة الـ CDN العالمية المجانية وسرعة الفتح الفائقة.
* **محرك المعالجة والذكاء الاصطناعي (OCR Backend)**: تطبيق FastAPI المعتمد على نماذج PyTorch و EasyOCR العصبية (حجمها قرابة 1.5 جيجابايت). نظراً لأن دوال Vercel Serverless تفرض حداً أقصى للحجم (250 ميغابايت فقط)، فإن السيرفر يُرفع على أي منصة سحابية تدعم استهلاك المعالج الكامل مثل **Render** أو **Railway** أو **Hugging Face Spaces** أو أي **VPS**.

---

## 1️⃣ الخطوة الأولى: رفع الواجهة الأمامية على Vercel

يمكنك رفع الواجهة على Vercel في دقيقة واحدة بإحدى طريقتين:

### الطريقة (أ): عبر موقع Vercel و GitHub (الأسهل)
1. قم برفع هذا المستودع إلى حسابك على **GitHub**.
2. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard) وسجل الدخول.
3. اضغط على **"Add New Project"** ثم اختر مستودعك من GitHub.
4. في صفحة الإعدادات:
   - **Framework Preset**: اختر `Other`.
   - **Root Directory**: اتركه الافتراضي `./` (ملف `vercel.json` جاهز ومعد تلقائياً).
5. اضغط على **"Deploy"**.
6. مبروك! سيصبح لديك رابط موقع مباشر مثل: `https://your-ocr-app.vercel.app`.

### الطريقة (ب): عبر الـ Terminal باستخدام Vercel CLI
```bash
# تثبيت Vercel CLI إذا لم يكن مثبتاً
npm install -g vercel

# تسجيل الدخول والرفع المباشر
vercel --prod
```

---

## 2️⃣ الخطوة الثانية: رفع سيرفر الـ OCR (Backend)

اختر المنصة المفضلة لديك لرفع سيرفر FastAPI:

### الخيار 1: النشر على Render.com (مجاني وسهل جداً)
1. ادخل على [Render.com](https://render.com) وأنشئ حساباً.
2. اضغط **New +** ثم اختر **Web Service**.
3. اربط مستودع الـ GitHub الخاص بك.
4. املأ البيانات التالية:
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
5. اضغط **Create Web Service**. بمجرد اكتمال الرفع ستحصل على رابط السيرفر، مثلاً:
   `https://my-ocr-backend.onrender.com`

---

## 3️⃣ الخطوة الثالثة: ربط الواجهة بالسيرفر

بمجرد فتح موقعك على Vercel:
1. انقر على أيقونة **⚙️ إعدادات السيرفر** في أعلى يمين الصفحة.
2. الصق رابط السيرفر الخاص بك (مثلاً: `https://my-ocr-backend.onrender.com`).
3. اضغط **🩺 اختبار الاتصال** للتأكد من جاهزية المحرك.
4. اضغط **💾 حفظ الإعدادات**.

> 💡 **ملاحظة**: يتم حفظ الرابط تلقائياً في المتصفح، وسيتصل موقعك بالسيرفر فورياً لمعالجة البطاقات والمستندات واستخراج البيانات بدون أي إعدادات إضافية!
